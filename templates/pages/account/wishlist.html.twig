{% extends 'base.html.twig' %}

{% block title %}My Wishlist - Vice City E-Shop{% endblock %}

{% block body %}

<div class="account">
    <div class="container">
        <div class="account__grid">
            <aside class="account__sidebar">
                <nav class="account__sidebar-nav">
                    <li><a href="{{ path('account') }}">👤 Profile</a></li>
                    <li><a href="{{ path('account_orders') }}">📦 Orders</a></li>
                    <li><a href="{{ path('account_wishlist') }}" class="active">❤️ Wishlist</a></li>
                    <li><a href="#">⚙️ Settings</a></li>
                </nav>
                <div class="account__sidebar-logout">
                    <button>🚪 Logout</button>
                </div>
            </aside>

            <div class="account__content">
                <div class="account__content-header">
                    <h1>My Wishlist</h1>
                    <p>{{ wishlist|length }} items saved</p>
                </div>

                <div class="wishlist__grid product-grid">
                    {% for item in wishlist %}
                    <div class="card">
                        <div class="card__image">
                            <img src="{{ item.image }}" alt="{{ item.title }}" loading="lazy">
                        </div>
                        <div class="card__content">
                            <h3 class="card__title">{{ item.title }}</h3>
                            <p class="card__description">{{ item.description }}</p>
                            <div class="card__meta">
                                <div class="card__price">${{ item.price }}</div>
                            </div>
                            <div class="card__footer">
                                <button class="btn btn--primary btn--full" data-add-to-cart='{{ item|json_encode }}'>
                                    Add to Cart
                                </button>
                                <button class="btn btn--outline">Remove</button>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
